# Finance Viewer 設定範本。
# 複製為 .env（已 gitignore，不會進 repo）後依需求修改；未設定則用預設值。

# SQLite 資料庫路徑（絕對路徑，或相對於專案根目錄）。預設 data/finance.sqlite。
# data/ 已被 .gitignore 排除，真實 DB 絕不會進 repo。
# FINANCE_DB_PATH=data/finance.sqlite

# Dev server port（預設 3127）。
# 注意：npm scripts 內的 `-p 3127` 目前是寫死的；Windows cmd 不展開 shell 變數，
# 若要改 port，最直接是編輯 package.json 內的 -p 值。
PORT=3127
