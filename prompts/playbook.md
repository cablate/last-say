# Finance Viewer Legacy Playbook Redirect

> This compatibility file is intentionally not the operator contract.

The complete, self-contained contract for an external AI operator lives in:

```text
.claude/skills/finance-viewer-ops/SKILL.md
```

Read that `SKILL.md`, then follow its reference routing. It contains the monthly import workflow, API contract, categories, confidence policy, bank-format quirks, search process, privacy rules, report operations, and the required learning loop.

Do not combine this file with an older cached playbook. The Skill directory is the only source of truth for Finance Viewer data operations.

For code modification, architecture, tests, and repository invariants, read `AGENTS.md` instead.
